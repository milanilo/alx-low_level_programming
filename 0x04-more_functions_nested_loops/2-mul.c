#include<stdio.h>
#include"main.h"
/**
 * mul - calcule the multiplication of tow numbers
 * @a: first number
 * @b: seconde number
 * Return: mult of a*b
 */
int mul(int a, int b)
{
	return (a * b);
}

