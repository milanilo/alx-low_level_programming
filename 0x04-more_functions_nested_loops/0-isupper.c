#include<stdio.h>
#include"main.h"
/**
 * _isupper - it checks a caracter is uppercase or not
 * @c: caracter cheked
 * Return: 1 if true and 0 if false
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (0);
		_putchar('\n');
	} else
	{
		return (0);
	}
}

